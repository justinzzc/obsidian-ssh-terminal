## 1. 回归测试

- [x] 1.1 增加工具栏回归测试：主机信息在连接后仍可见，且只有一个连接相关按钮随生命周期切换状态和操作。
- [x] 1.2 增加 Escape 回归测试：事件不被取消，也不会调用 `returnFocus`。
- [x] 1.3 运行定向测试并记录失败，确认测试能复现两个 bug。

## 2. 最小修复

- [x] 2.1 重构 `TerminalView` 为单一连接按钮，并保持主机标签稳定。
- [x] 2.2 删除 Escape 劫持与不再需要的 reconnect 逻辑。
- [x] 2.3 运行定向测试、完整测试和生产构建。

## 3. 编辑模式会话保持

- [x] 3.1 增加 RED 测试：进入编辑模式后返回，连接字段不变时复用原会话，height 变化不关闭会话。
- [x] 3.2 增加连接目标等价判断与 `SessionManager.resume`，连接字段变化时关闭旧会话。
- [x] 3.3 调整实时预览和 `TerminalView` 生命周期：临时销毁只拆 UI，返回时恢复；block 删除和编辑器销毁仍关闭会话。
- [x] 3.4 运行完整类型检查、单元测试和生产构建。
